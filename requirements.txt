# Core Dependencies
python-dotenv>=1.0.0

# LangChain Ecosystem
langgraph>=0.2.0
langchain-openai>=0.2.0      # For LM Studio / OpenAI-compatible API
langchain-chroma>=0.1.0
langchain-huggingface>=0.1.0
langchain-community>=0.3.0
langchain-core>=0.3.0
langchain-text-splitters>=0.3.0

# Vector Database
chromadb>=0.5.0

# Embeddings
sentence-transformers>=2.7.0

# Document Processing & OCR
pypdf>=4.0.0
pdf2image>=1.16.0            # Converts PDF pages to images for OCR fallback
pytesseract>=0.3.10          # Python wrapper for Tesseract OCR engine
Pillow>=10.0.0               # Image processing library (PIL) for standalone images

# Web Search & Scraping
duckduckgo-search>=6.0.0     # Powers DuckDuckGoSearchRun
beautifulsoup4>=4.12.0       # Powers WebBaseLoader for HTML parsing
requests>=2.31.0             # HTTP library used by web loaders